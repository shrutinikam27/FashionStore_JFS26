# ==========================================
# Application Settings
# ==========================================
server.port=8080

# Use H2 in-memory database
spring.profiles.active=

# ==========================================
# H2 Database Configuration
# ==========================================
spring.datasource.url=jdbc:h2:mem:fashiondb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# ==========================================
# File Upload
# ==========================================
file.upload-dir=uploads

# ==========================================
# Jackson Configuration
# ==========================================
spring.jackson.serialization.fail-on-empty-beans=false

# ==========================================
# JWT Configuration
# ==========================================
jwt.secret=ZmFzaGlvbnN0b3Jlc2VjdXJla2V5ZmFzaGlvbnN0b3Jlc2VjdXJla2V5ZmFzaGlvbnN0b3Jlc2VjdXJla2V5
jwt.expiration=86400000

# ==========================================
# Gmail SMTP Configuration
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nikamshruti27@gmail.com
spring.mail.password=sthdyamdahrdmzqo

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Enable debug logs
spring.mail.properties.mail.debug=true

# ==========================================
# Default From Email
# ==========================================
spring.mail.from=nikamshruti27@gmail.com

# ==========================================
# Disable Resend (Using Gmail Only)# Application settings
server.port=8080

# Use H2 in-memory database (always seeded with products on startup)
# To switch to MySQL: change this to spring.profiles.active=mysql
# and ensure MySQL is running with fashion_db schema created
spring.profiles.active=

# Enable H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

# File upload properties
file.upload-dir=uploads

# Spring Jackson config to ignore empty Hibernate proxies
spring.jackson.serialization.fail-on-empty-beans=false

# JWT Security Configurations
# 256-bit base64 encoded secret key: "fashionstoresecurekeyfashionstoresecurekeyfashionstoresecurekey"
jwt.secret=ZmFzaGlvbnN0b3Jlc2VjdXJla2V5ZmFzaGlvbnN0b3Jlc2VjdXJla2V5ZmFzaGlvbnN0b3Jlc2VjdXJla2V5
# 24 hours in milliseconds
jwt.expiration=86400000

# ==========================================
# Default H2 Database Configuration
# ==========================================
spring.datasource.url=jdbc:h2:mem:fashiondb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none

# Run schema.sql and data.sql on startup
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# ==========================================
# SMTP Mail Server Configuration (Gmail)
# ==========================================
# Replace 'YOUR_GMAIL_ADDRESS' with your actual Gmail account.
# Replace 'YOUR_GMAIL_APP_PASSWORD' with a 16-character App Password.
# Note: Do not use your primary password. Generate an App Password via:
# https://myaccount.google.com/apppasswords
spring.mail.host=${SPRING_MAIL_HOST:smtp-relay.brevo.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:nikamshruti27@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:sthdyamdahrdmzqo}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.debug=true

# ==========================================
# Resend Email Configuration (HTTP API fallback)
# ==========================================
resend.api.key=${RESEND_API_KEY:re_N2vd8FYT_KFmHeK2faY6RDdYRhVwBMfEh}
resend.from.email=${RESEND_FROM_EMAIL:Fashion Store <noreply@mydomain.com>}

# ==========================================
# Stripe Payment Configuration (Test Mode)
# ==========================================
stripe.api.key=YOUR_STRIPE_SECRET_KEY
stripe.publishable.key=YOUR_STRIPE_PUBLISHABLE_KEY


# ==========================================
resend.api.key=
resend.from.email=nikamshruti27@gmail.com

# ==========================================
# Stripe (Test Mode)
# ==========================================
stripe.api.key=YOUR_STRIPE_SECRET_KEY
stripe.publishable.key=YOUR_STRIPE_PUBLISHABLE_KEY